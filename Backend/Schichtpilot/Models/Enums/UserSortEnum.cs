namespace Schichtpilot.Models.Enums;

/// <summary>
/// After what the users can be sorted when requesting a list of them.
/// </summary>
public enum UserSortEnum
{
    Id,
    FirstName,
    LastName
}